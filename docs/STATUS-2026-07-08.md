# DraftHQ — Status & Handoff

> **SUPERSEDED by [`STATUS-2026-08-06.md`](STATUS-2026-08-06.md).** Kept for
> history. The deployment table below was verified and corrected on 2026-08-06;
> the "what's next" list has moved on. Read the newer doc.

_Last updated: 2026-07-08. Branch: `feature/design-system-overhaul` @ `4422596`._

---

## What DraftHQ is

A **premium draft-night companion**, not a Sleeper/ESPN replacement. Commissioners
import their existing league, run a far better draft night in DraftHQ, then go
back to their platform for the season.

Every product decision should answer: *"why would a commissioner run draft night
here instead of in their platform's built-in draft?"* Optimize for: frictionless
import, an unforgettable event, and easy export back. Do **not** build league
history, standings, or year-round engagement.

---

## Current state

**Branch:** `feature/design-system-overhaul` — **not merged to `main`.** It carries
both Codex's design-system/TV-mode work and the draft-grading feature. Decide
whether those ship together or get split before opening a PR.

**Verification gate** (run all of these before considering work done):

```bash
npm run typecheck && npm run test:unit && npm run build && npm run lint:errors
```

Current: typecheck clean, **145 tests passing**, build clean.
`lint:errors` reports **8 pre-existing** `react-hooks/set-state-in-effect`
violations documented in `docs/stabilization-2026-06-29.md`. Treat 8 as the
baseline — any new error is yours.

---

## Deployment — verified live 2026-08-06

Schema was probed directly against project `kogyejhzzggrkekbcppm`. Note that
`supabase_migrations.schema_migrations` is **empty** — everything was applied by
pasting into the SQL editor, so `list_migrations` reports nothing. Verify by
probing for columns and buckets, not by trusting that table.

| Migration | What it does | Status |
|---|---|---|
| `20260630000000_walk_up_music_mode` | Walk-up resume/restart setting | ✅ `drafts.walk_up_music_mode` present |
| `20260630000001_announcer_clips_bucket` | `announcer-clips` storage bucket | ✅ bucket exists |
| `20260630000002_landmine_videos_bucket` | `draft-videos` bucket for landmine clips | ✅ bucket exists |
| `20260630000003_awards_song` | Awards ceremony song column | ✅ `drafts.awards_song` present |
| `20260630000004_rankings_adp_projections` | `adp` + `projected_points` on `espn_rankings` | ✅ present |
| `20260806000000_league_assets_bucket` | `league-assets` bucket + commissioner RLS | ✅ applied 2026-08-06 |

**Rankings sync has run.** 2,400 of 4,848 `espn_rankings` rows carry ADP with a
1.9–171.6 spread — a real distribution, not ESPN's flattened end-of-season
constant. The grader is getting the good signal, not the consensus-rank fallback.

**Env vars (Vercel):**
- `RANKINGS_SYNC_SECRET` — required, or the sync 503s and ADP never lands
- `SUPABASE_SERVICE_ROLE_KEY` (or `SUPABASE_SECRET_KEY`) — should already be set
- `OPENAI_API_KEY` — **not needed**; AI announcers are disabled (see below)
- `NEXT_PUBLIC_AI_ANNOUNCER_ENABLED` — unset. Set to `true` + redeploy to enable AI voices

---

## Recently shipped

### Draft grading (`src/lib/draftGrading.ts`)
Per-pick 0–100 scores and overall team grades, all judged on what was knowable
**at the time of the pick** — the board and each roster are replayed pick by pick.

Five weighted factors: **value vs market 40%**, **team need 20%**, **positional
scarcity 15%**, **player quality 15%**, **roster construction 10%**.
Team grade = 70% round-weighted picks + 20% final construction + 10% strategy.
Calibrated so an ordinary draft lands **C+/B-**, guarded by
`tests/unit/draftGradingCalibration.test.ts` which simulates a full draft.

Three bugs were found by simulation and fixed — **do not reintroduce**:
1. K/DST are ranked ~290–320 overall but drafted in the last two rounds. Judged
   against overall rank that reads as a 150-pick reach. Value is now skipped for
   them unless real ADP exists.
2. Quality used overall rank, punishing every late pick for being late. Now
   measured **within position**.
3. Need counted unfilled K/DST slots as pressing all draft, while construction
   penalized drafting them early — the engine contradicted itself. They only
   become pressing in the final rounds.

UI: **Draft Grades** board view (complete drafts only) with per-team summaries,
strengths/concerns, and every pick's score + explanation. Awards ceremony finale
shows the real grades.

### Create Draft modal
Takes an **optional draft date + time**, so the countdown can exist from the
moment the draft does. It's a post-create `updateDraftSchedule()` call rather
than a new RPC parameter — `create_draft_for_season` is unchanged, so **no
migration**. A failed schedule save is swallowed: the draft already exists at
that point, and the commissioner lands on draft settings next, where the same
fields live. Also fixed a mojibake em dash in the modal title (`â€"`).

### Data pipeline
- ESPN **ADP** + **projected points** captured in the rankings sync. ESPN
  flattens ADP to a constant once a season ends — the sync detects the
  degenerate spread and discards it rather than feeding the grader a fake signal.
- **Sleeper `roster_positions` is now parsed** (it was fetched and thrown away —
  only `.length` was used). Gives real starting lineups, FLEX/SUPER_FLEX, bench
  size, and scoring format on imported drafts. TE premium is detected and warned
  about since DraftHQ can't model it.

### Draft night experience
TV/broadcast mode, awards ceremony (one award per screen, music, league logos),
landmine videos, walk-up resume/restart mode, draft-order race lottery,
lobby presence + volume, mobile owner audit, Draft Complete modal.

---

## Known gaps and limitations

**Data the grader cannot use** (honest limits, not omissions):
- No floor/ceiling/risk/role data — "upside" is inferred from rank and round
- TE premium isn't a supported scoring type
- Keeper/dynasty not in the schema
- Projections are PPR-scored from ESPN's default league regardless of format

**AI announcers are disabled everywhere** (`isAiAnnouncerEnabled()` in
`src/lib/speech.ts`). All plumbing is intact — OpenAI personas, BYO-ElevenLabs
with per-request keys, storage-cached clips, device-TTS fallback. Flip
`NEXT_PUBLIC_AI_ANNOUNCER_ENABLED=true` to relaunch.

**Manually-created drafts still have no starting lineup** — every `min` defaults
to 0. The grader infers a standard lineup and labels it as assumed. Sleeper
imports are now exact.

**Stabilization leftovers** (from `docs/stabilization-2026-06-29.md`):
8 deferred lint errors, no rate limiting on public endpoints, Spotify tokens in
localStorage. (The `league-assets` RLS item is resolved — see below.)

**League logos were never actually uploaded.** The `league-assets` bucket did
not exist, so `uploadLeagueAsset` silently caught the storage error and fell
through to a canvas fallback: resize to **256px**, encode as base64, store the
data URL in `leagues.logo_url`. That is the real cause of blurry logos on the
TV — not small source images. The existing league carried a **206KB** data URL
shipped on every workspace load.

Fixed 2026-08-06: bucket created with commissioner-scoped RLS at path
`{leagueId}/{type}.{ext}`, uploads now fail loudly instead of degrading
silently, and the returned URL is cache-busted since the path is stable.

⚠️ **Existing logos are not recoverable by migration.** The stored data URL is
already downsampled to 256px; the original file is gone. Each commissioner must
**re-upload their logo once** to get a full-resolution hosted asset.

---

## What's next — suggested order

### ~~1. Non-commissioner dashboard~~ ✅ shipped
Owners (`!workspace.canManage`) now get their own dashboard instead of the
commissioner's. `src/lib/ownerDashboard.ts` derives the owner view as pure logic
(covered by `tests/unit/ownerDashboard.test.ts`); `LeagueCommandCenter` renders
it. What changed for an owner:

- Hero eyebrow reads **Draft Night**, badge reads the draft's own state
  (Draft Scheduled / Draft Live / Draft Paused / Draft Complete / Date TBD /
  Draft Not Created) — never "33% READY"
- The **"Next Commissioner Action"** panel is replaced by **"Your Team"**: team
  logo and name, assigned/awaiting status, draft slot, rounds, and a plain-English
  note when the slot or assignment is still out of their hands
- **There is always a button.** A draft existing is enough — the lobby handles
  pre-draft — so owners get Enter/Join Draft Room regardless of setup readiness,
  falling back to the league team list when no draft exists. Secondary action
  links to the existing `/leagues/[slug]/my-team` page
- The readiness checklist is gone; the **countdown panel spans the row**, and its
  unscheduled state tells owners what to expect rather than telling them to
  schedule something they can't
- Hero metrics become Draft Status / Your Pick / Pick Clock / League members

Draft slot comes from `getLeagueTeamDraftSlot()` reading `league_team_seasons`,
which league members can already select under existing RLS — **no migration
needed**. It fails soft to "TBD".

### 2. Export / sync back to Sleeper  ← biggest strategic gap
The companion thesis is import → great draft night → **back to your platform**.
Only a CSV download exists. This closes the loop and makes DraftHQ
zero-commitment. Sleeper's API is already integrated for import.

### 3. Merge or split the branch
Decide whether Codex's design-system work and draft grading ship together.

### 4. Smaller items
- Grader v2: roster-construction penalty for unfilled starters in the *pick*
  need score; capture `durationSeconds` in the song picker for exact multi-song
  walk-up resume
- Order race v2: broadcast the race to every device (seed + start timestamp,
  same pattern as walk-up sync); announcer play-by-play once AI voices launch
- `league_events` table so League Activity shows joins **and** leaves
- Logo-dimension warning on upload (small source images look blurry on the TV)
- Transactional email setup (long-deferred)

---

## Architecture notes worth knowing

**Audio is the most fragile subsystem.** It was audited and hardened; the
invariants matter:
- Playback position is **derived**, never stored — every client computes the same
  answer from pick timestamps, which is why walk-up music stays in sync
- One `walkUpDelayRef` timeout slot; every transition clears it before scheduling
- Reveal and landmine ownership is enforced at **fire time** inside delayed
  closures (`revealActiveRef`, `landmineActiveRef`), not just at schedule time
- `WalkUpPlayer.play()` reloads from the top — never call it to "resume"

**Grading is cached** against `picks · rankings · scoring · rounds` and only
computed when the grades view or ceremony is open. It walks every pick against
the whole board.

**Draft correctness lives in Supabase/Postgres.** React displays state; it does
not enforce draft rules. Don't use localStorage as authoritative draft state.

**`update_draft_extras` is recreated by several migrations.** Adding a parameter
means dropping and recreating the whole function and updating the `revoke`/`grant`
signatures. The client always sends every parameter, so code and migration must
deploy together.

---

## Quick smoke test after deploying

1. Draft settings → toggle any presentation setting → saves (proves the RPC migrations)
2. Teams tab → reorder with arrows → Save order → refresh → order held
3. Finish a mock draft → board switcher gains **Draft Grades** → expand a team → per-pick explanations
4. Awards ceremony → music plays, grades shown on the finale
5. Lobby on a phone → join code visible, online count ticks

---

_Superseded. Current status lives in [STATUS.md](STATUS.md)._
